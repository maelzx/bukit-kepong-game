# assets/

Intentionally empty. Everything in the game is drawn procedurally on the
HTML5 canvas and every sound is synthesised at runtime with the Web Audio API,
so the prototype runs from `file://` with no external or licensed assets.
